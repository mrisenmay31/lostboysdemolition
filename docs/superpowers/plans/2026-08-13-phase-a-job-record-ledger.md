# SDD ledger — plan: docs/superpowers/plans/2026-08-13-phase-a-job-record.md

> **Frozen session ledger, closed 2026-08-13.** Committed verbatim from
> `.superpowers/sdd/2026-08-13-phase-a-job-record/progress.md` (that path stays local-only,
> along with the task briefs/reports in the same session directory). This file is the
> permanent, in-repo record of every ruling, defect, and review round from the Phase A build
> session — do not edit it retroactively; append-only history for a closed session.

Branch: phase-a-job-record (from main @ 810877c)
Execution model (Matt): orchestrator advises only; implementers = sonnet; task reviewers = opus (adversarial). Matt checkpoints after Task 1, after Task 4 (before real crew channels), at Task 6.
Spec: BUILD_PLAN.md → Revised phases → Phase A + Matt's 2026-08-13 decisions (GHL trigger; JOB-XXXX – Client – City).

## Pre-flight conflict scan (2026-08-13)

| Pair / Task | Produces vs consumes | Finding |
|---|---|---|
| T1 ↔ T3/T4/T5 | T1 interface line says column `status job_status`; T1 SQL creates `status_v2` (legacy `status` enum already exists on jobs). T3 inserts `status_v2='accepted'`; T4 updates `status_v2='scheduled'`; T5 queries `status_v2='scheduled'` | Interface line is a typo; SQL + all consumers agree on `status_v2`. **Ruling: `status_v2` governs.** Cost if wrong: column-name drift in one task — caught by tests/verify. |
| T1 interface ↔ T1 SQL | Interface says `ghl_opportunity_id text UNIQUE NOT NULL`; SQL adds UNIQUE, no NOT NULL | **Ruling: NOT NULL deliberately omitted** (legacy columns/rows coexist during parallel running; every new-code insert supplies it; UNIQUE gives the idempotency guarantee). Cost if wrong: a null-opp row could slip in via manual insert — tolerable. |
| T2 lift range | "lift 141-235" but `formatCurrency` is at :237 | Named-function list governs, not the line range. No ruling needed. |
| T3 ↔ T4 | Same file `ghl-job-webhook/index.ts`, sequential tasks, T4 dispatched only after T3 review passes | No conflict. |
| T4 ↔ T5 | Slack secrets `SLACK_CREW1..4_CHANNEL` + message format shared | Consistent; T5 consumes T4's helpers. |
| T0 | Docs only | Self-consistent. |
| T1 regex | `'^JOB-\d{4,}$'` in Postgres check | Valid ARE syntax in Postgres. No conflict. |
| Rubric vs plan | No plan-mandated test-that-asserts-nothing or verbatim duplication found; google.ts lift is deliberate reuse-by-extraction, not duplication (old function untouched per Global Constraints — accepted transitional duplication until Airtable functions retire) | **Ruling: transitional duplication accepted**; cost: two copies of Google auth until Phase-B-era cleanup. |

## Entries
Task 0: minor (deferred): CLAUDE.md Supabase-tables row still says sync_log 668 (line ~43 now says 918) — fold into Task 7 docs pass
Task 0: minor (deferred): "Where the project stands (2026-07-30)" heading now contains an Aug-13 figure — heading-date bump in Task 7
Task 0: complete (commits 810877c..0aac400, review clean; trailer verified by controller)
Task 1: Ruling: classifier blocked delegating live migration apply to a subagent — split task: subagent authors+commits the file; controller applies via Supabase MCP in-session (Matt's permission prompt = the after-Task-1 checkpoint). Cost if wrong: none material — same SQL, more oversight.
Task 1: Ruling: PLAN DEFECT caught pre-apply — enum type `job_status` already exists live with values {active,archived} (legacy jobs.status column). Plan's `create type job_status ... when duplicate_object then null` would silently bind status_v2 to the WRONG enum; Task 3 inserts of 'accepted' would fail at runtime. New enum renamed `job_lifecycle` (same 7 values); status_v2 typed job_lifecycle; Tasks 3/4/5 unchanged (they reference the column, not the type). Cost if wrong: none — strictly safer; plan file annotated.
Task 1: migration applied to live by controller (Matt-visible MCP permission = checkpoint). Verify: mint=JOB-1100, jobs 0 rows, backup 7 rows, RLS true on both, status_v2 type=job_lifecycle. Note: 2 pre-existing policies on jobs (authenticated_select_jobs, admins_manage_jobs) — anon-safe (require auth.uid()/admin role), anticipate Phase-4 PWA, kept. jobs_legacy_backup has 0 policies.
Task 1: review found 1 Critical + 7 Important. Controller verified Critical REAL: jobs.airtable_job_id is NOT NULL, no default → canonical inserts would 23502. job_name NOT NULL is fine (always supplied); status defaults 'active'.
Task 1: Ruling: fixes ship as NEW migration phase_a_jobs_keystone_fixups (applied migrations are never edited — parity rule wins over replay-hygiene findings #2/#3/#4; replay risk is bounded by Supabase migration versioning; documented in Task 7 BUILD_LOG entry). Cost if wrong: repo migration set stays non-replayable on fresh DB — accepted, house pattern.
Task 1: Ruling (#5): Task 3 must validate opportunityId non-empty before insert; ghl_opportunity_id always populated in the Phase A flow → NULL-race non-issue. Carried into Task 3 dispatch.
Task 1: Ruling (#6): receive-airtable-webhook pollution of jobs accepted during parallel running (legacy rows have status_v2 NULL → invisible to new queries); retirement already decided by Matt, queued as its own task. Surface to Matt at next checkpoint.
Task 1: Ruling (#7, reviewer challenged controller): agree — drop BOTH pre-existing policies (authenticated_select_jobs, admins_manage_jobs) in fixups migration; restores documented no-policies posture; Phase-4 PWA recreates scoped policies when real users exist. Cost if wrong: PWA work must re-add policies — trivial.
Task 1: Ruling (#9): no trigger; Task 4's UPDATE sets updated_at=now() explicitly. Carried into Task 4 dispatch.
Task 1: minor (deferred): enum guard checks name not values (#10); no schema qualification (#11); no status_v2 index (#12); report inaccuracies (#13).
Task 1: fix round 1/5 (3 findings targeted R1/R7/R8; commit 7fca329; controller applied fixups live; trial canonical insert OK → JOB-1101/accepted/NULL airtable_job_id, rolled back; policies 0; search_path pinned). Scoped re-review dispatched.
Task 2: implementer DONE (commit 55c17f6, 4 test groups passing, deno check clean, parseCity passed all cases unmodified). Opus review dispatched.
Note: trial insert consumed JOB-1101; next mint is JOB-1102 — sequence gaps are fine, never reset.
Task 1: complete (commits 5c52c8b + 7fca329, fix round 1 re-review clean; both migrations applied live and verified; parity held — applied keystone file untouched)
Task 1: minor (deferred): dual status columns (legacy status 'active' + status_v2) written side-by-side until Airtable retirement; search_path='public' is the weaker-but-passing advisor variant; jobs_legacy_backup holds the only copy of the 7 archived rows.
Task 2: review Approved w/ findings. ⚠️ job_events columns resolved by controller: live schema (queried this session) HAS job_number + ghl_opportunity_id — CLAUDE.md's column list is incomplete, writers are correct. Fold doc fix into Task 7.
Task 2: Ruling (#1): silent log-write failures REAL (supabase-js returns {error}, never throws) — plan-mandated code was wrong; fix in round 1 (check result.error + console.error, keep try/catch).
Task 2: Ruling (#3): parseCity returning "UT 84107" as city on 2-part and 4-part (trailing country) addresses — fix in round 1 with regression tests.
Task 2: Ruling (#2): suffix-less no-comma streets ("1234 S Main Sandy UT" → "Main Sandy") — ACCEPTED as heuristic limit; GHL/Google-autocomplete addresses are comma-separated so the no-comma branch is already the fallback path; a city whitelist is over-engineering at this stage. Cost if wrong: occasional ugly-but-harmless city segment in a job name, fixable in Phase B when address data firms up.
Task 2: Ruling (#7): status:'success' + action_taken:'skipped' for skipped ops — matches house pattern in deployed functions; intended. Carried into Task 3 dispatch.
Task 2: minor (deferred): no deno.json/lockfile (std@0.224.0 unpinned); no tests for log.ts/google.ts; compile-time-only action_taken enforcement (mitigated by #1 fix making violations self-reporting).
Task 2: fix round 1/5 (2 findings F1 log errors + F2 parseCity guards; commit 0b8f5b2; 9 parseCity assertions green, deno check clean). Scoped re-review dispatched.
Task 2: fix round 1 re-review: F1 ADDRESSED, F2 ADDRESSED, but NEW Important — parseCity throws on country-only input ("USA"). Round 2 dispatched (guard + 3 tests).
Task 2: minor (deferred): "123 Main St, UT 84107" yields "123 Main St" as city — wrong either way on city-less addresses, cosmetic in job names.
Task 2: fix round 2/5 (F3 addressed, 0 open; commit 358cf8a)
Task 2: complete (commits 55c17f6..358cf8a, 2 fix rounds, re-review clean; 12 parseCity assertions, deno check clean)
Task 3: implementer dispatched (BASE 358cf8a). Ruling: implementer builds+tests+commits only; controller deploys via MCP and live-verifies (classifier precedent from Task 1). Live-verify needs GHL_WEBHOOK_SECRET value from Matt (or Matt runs the curl via `!`) — flagged in chat.
Task 3: implementer DONE (commit b6f0f27, 34/34 tests, 3 self-flagged concerns fed into review lens). Opus review dispatched on 358cf8a..b6f0f27.
Ruling (parallelization, Matt asked): T3→T4 strictly sequential (same file). T5 runs CONCURRENT with T4 — disjoint files; T5 implements its own digest builder against the plan's verbatim message format instead of importing T4 helpers (cost if wrong: ~15 lines of duplicated formatting, consolidate in Phase B). T6 first half (GHL workflow + payload capture) starts as soon as T3 deploys, parallel with T4 — needs Matt's GHL access. Slack channel lookup attempted via MCP: connected workspace is CTA Integrity, NOT Lost Boys — crew channel IDs must come from Matt.
Task 3: review Needs fixes — Critical C1 (23505 can't distinguish job_number collision from ghl_opportunity_id race → silent 200/skipped/success with no row) + I1 no outer try/catch (parseCity on non-string GHL shapes → unlogged 500), I2 race path untested (mock overrides written but never used, can't express pre/post-read divergence), I3 GHL PUT body diverges from working precedent (partial update may be rejected or wipe custom fields; retry never re-attempts PUT), I4 no sync_log on startup-500, I5 STARTUP_ERROR gates create path on the unused Job Scheduled stage ID, I6 empty-string custom field defeats address fallback.
Task 3: Ruling (I3): keep partial PUT until live evidence; ADD skip-path re-attempt of the GHL write-back (idempotent PUT) so re-fires self-heal; live-verify checklist must inspect PUT response AND re-GET the opp to confirm Job Address survived. Cost if wrong: one extra PUT per duplicate webhook fire — negligible.
Task 3: Ruling (I5): plan-mandated both-stages gate overridden — gate per-event (create path needs neither stage ID; schedule path 500s only if its own stage unresolved). Spec authority: Phase A's purpose is never losing a job creation. Cost if wrong: job_scheduled events fail slightly later rather than at startup — acceptable.
Task 3: minor (deferred): number gaps on non-race insert failure (accepted by plan); PII in logs (keep until live shapes confirmed, then trim — revisit before Task 6 close); non-timing-safe secret compare (fleet pattern); GHL-404→500 conflation; one padding test (field-ID non-empty).
Task 5: implementer dispatched CONCURRENT with Task 3 fix round (disjoint dirs; git-lock retry instructed). Rulings carried: no deploy/DB (controller applies); own digest builder (no _shared edits while parallel agent runs); DST via local-hour self-gate + dual UTC cron entries; secret-header placeholder __WEBHOOK_SECRET__ in committed migration (controller substitutes at apply — documented divergence); reuse GHL_WEBHOOK_SECRET for this function's auth; no 🕗/📞 lines (fields don't exist on jobs).
Task 5: implementer DONE (commit bd7aca7, 39/39, migration carries __WEBHOOK_SECRET__ placeholder). Opus review dispatched (b6f0f27..bd7aca7).
Task 3: fix round 1/5 (6 findings C1+I1-I6; commit 9fa8770; 48/48 tests). Scoped re-review dispatched (bd7aca7..9fa8770 to exclude interleaved T5 commit).
Task 3: fix round 1/5 re-review: ALL 6 ADDRESSED (C1 traced to correct 500+error logs; race mock genuinely diverges; 48/48 green), no new breakage. Live-verify checklist captured (startup logs, I4 forced-failure sync_log row, 401 no-log, custom-field key shape).
Task 3: minor (deferred→T4 dispatch): outer-catch hardcodes trigger_event 'quote_accepted' (mislabels job_scheduled failures) + omits payload_in; failed skip-path retry logs success in sync_log but error in job_events. Both in files T4 edits anyway.
Task 3: code complete pending deploy + live verify (controller). T4 unblocked for dispatch after deploy.
Task 5: review Needs fixes — I1 missing-env logs skipped/success (invisible misconfiguration; reviewer proved skipped groups are NEVER retried — hour gate kills 2nd fire, next day the date window moved), I2 case-sensitive crew grouping can split one crew into two messages, I3 cron 5s default timeout can abort mid-run, I4 cron POST carries no Authorization — function must deploy with --no-verify-jwt.
Task 5: Ruling (retry): keep single-send design — NO same-day retry machinery (gate widening + extra cron entries is baroque for a convenience layer; the calendar event from Task 4 is the primary signal). Recovery path = failed nights now VISIBLE as sync_log errors (I1 fix). Cost if wrong: a missed digest is seen but not auto-resent; foreman still has the calendar.
Task 5: Ruling (I4): controller deploys crew-night-before with --no-verify-jwt (same as fleet); migration header gains a note. No auth-header change.
Task 5: minor (accepted into fix round, cheap): .order('job_number'); per-group try/catch for total isolation; 2 DST-transition-day assertions. Deferred: sync_log silence-on-no-op note → Task 7 BUILD_LOG.
Task 5: fix round 1/5 re-review: ALL ADDRESSED (F1-F4 + minors a-c), 41/41, no new breakage. Code complete pending controller apply/deploy.
LIVE DEFECT (found by controller probe with real secret, bogus opp): 500 response correct BUT both audit writes 400 — sync_log_direction_check allows only the 2 Airtable directions (new code sends ghl_to_supabase / supabase_to_slack); job_events.job_id is NOT NULL (legacy col new code intentionally omits). Mocks can't see DB constraints — live probe caught it. Third migration dispatched (widen check + drop NOT NULL). CLAUDE.md documents only action_taken check → document direction check in Task 7.
Secret received from Matt via ! grep (never rotated — ghl-contact-sync depends on it). Auth verified current: bogus-opp POST → 500 with GHL 404 detail; wrong-secret → 401 no logs.
Third migration applied (audit-write fixups): re-probe → 500 with BOTH audit rows landing (sync_log ghl_to_supabase/error + job_events error). Error path fully live-verified.
Task 5: controller half done — crew-night-before deployed --no-verify-jwt; cron migration applied with secret substituted (repo file keeps placeholder); cron.job shows both entries active (22:30/23:30 UTC); force smoke test → 200 "no jobs" with tomorrow=2026-08-14 (Denver-local math correct).
Task 5: complete (commits bd7aca7 + 4942552 + deploy; re-review clean; live smoke-tested). Slack legs remain unverified until SLACK_CREW*_CHANNEL secrets exist (Matt).
Task 4: implementer DONE (commit 0f3c6a9, 88/88). Ruling (concern 3): NO stage PUT needed — the GHL workflow trigger IS the stage change; the opp is already in Job Scheduled when the webhook fires. Ruling (concern 2, pre-review): the spirit of per-leg idempotency is per-EVENT-ID — crew event must be creatable on re-fire when main exists but crew is null; expect the reviewer to confirm; goes to fix round. Concern 1 (BILL endpoint unverified): accepted — gated leg, no creds exist; verify against developer.bill.com when Matt supplies creds (Phase C at latest).
Task 4: review Needs fixes — C1 calendar leg not per-event-ID resumable, C2 mirror case duplicates crew events on live calendars (same root cause), I1 configured-crew-without-calendar-env masked as success, I2 side effects precede the single DB write + created event IDs never logged (orphan risk), I3 reschedule divergence (stamped legs skip while dates changed), I4 cleanup 8b over-corrected (no-op skips now logged 'updated').
Task 4: Ruling (I3): Phase A ships reschedule VISIBILITY, not automation — when crew/start/end differ from stored values while legs are stamped: update DB, write job_events 'reschedule_detected' with old→new in payload, response calendar:'stale', console.error. Full auto-reschedule (patch/move events, re-notify) is a surfaced backlog item for Matt — today reschedules are hand-managed anyway (parallel running). Cost if wrong: manual calendar fixes continue on reschedules, same as today, but now visibly logged.
Task 4: minor (deferred): GHL DATE fields may arrive as epoch ms → resolve at Task 6 payload capture before trusting the guard; bill_job_code=job_name intentional (BILL selector value is the human-pickable name; Phase C must match exact en-dash string); no concurrency guard on simultaneous fires; benign-skip reasons in error_message on success rows.
LIVE E2E (create path): Matt supplied test opp OQzr5dwMbqpuOBKf5xsD → 200 created JOB-1102 "JOB-1102 – Contractor Company", client_type Contractor from tags, ghl_update success (PARTIAL PUT ACCEPTED by GHL — I3 risk #1 settled). Re-fire → skipped/same number/PUT re-ran. City correctly omitted (no Job Address on opp). Custom-field-wipe check moot on this opp — re-test at Task 6 with populated Job Address. Awaiting Matt's visual confirm of renamed opp in GHL UI.
Task 4: fix round 1/5 re-review: ALL ADDRESSED (C1+C2 both directions traced+tested, I1-I4), no new breakage, 94/94.
Task 4: minor (deferred, from re-review): unset GOOGLE_CALENDAR_MAIN with configured crew still bare 'success' (I1 asymmetry, pre-existing); slack_notified_at/bill_job_code stay terminal-write → terminal failure after Slack post = duplicate crew message on retry (gcal-style prompt-persist scoped to gcal only); I2+I3 interaction can log one spurious reschedule_detected after a terminal-failure retry (noise only); partial-stamp reschedule can leave main/crew events at different dates ('stale' letter-of-ruling). Final review triages.
Task 4: complete (commits 0f3c6a9 + f63be73, fix round 1, re-review clean)
Task 3: complete (deploy + live E2E: JOB-1102 minted from real opp, idempotent re-fire, error paths verified; Matt's GHL-UI visual confirm outstanding)
Matt supplied 4 crew channel IDs; SLACK_CREW1..4_CHANNEL secrets set (Crew1 C087S6M0Q4Q Nick, Crew2 C087S6G3248 Alex, Crew3 C0ABF44937A Brady, Crew4 C0ABF4XMKDE Cade). Live-Slack E2E pending Matt's call: real-channel test message vs temporary test channel.
Matt's screenshot confirms GHL UI: opportunity card renamed "JOB-1102 – Contractor Company". Create path visually verified end-to-end.
Task 6: Matt created both GHL workflows + dragged test opp to Quote Accepted. Payload capture: 400 — GHL "Webhook" action nests custom data under customData key. Fix dispatched (accept both shapes + keys-only diagnostic on rejection).
Task 6 findings: LIVE pipeline has 12 stages not 13 (no "Closed Lost (Cancelled)") — CLAUDE.md stage table wrong, fix in Task 7. Slack E2E to ops-test succeeded earlier (ok:true, exact message shape confirmed, 📍 omitted when address null — correct). BONUS LIVE BUG (out of scope, surface to Matt): deployed ghl-contact-sync v20 threw unlogged TypeError tags.map-not-a-function on live traffic 22:24 — pre-existing function, not Phase A code; needs its own small fix later.
customData envelope fix: re-review ADDRESSED, no new breakage, 98/98 (commit 402b6b0). Redeployed ghl-job-webhook. Note (deferred): PII in [ghl] contact fetch console log — pre-existing debug logging kept until Phase A shapes fully confirmed, then trim (already ledgered under Task 3 minors).
Task 6: ACCEPTANCE PASSED — Matt's UI drag drove the full path (22:34:50 sync_log: quote_accepted/skipped/success, opportunityId extracted from customData envelope). Workflow 2 (job_scheduled) drag pending; JOB-1102 cancellation question posed to Matt (digest would fire to real Crew 1 channel Aug 16).
Task 7: docs commit 939429f. Airtable Secrets & Credentials updated by controller (4 SLACK_CREW rows, ✅ Live) — agent's open-item note in NEXT_SESSION_PROMPT about Airtable is STALE, verify in final wave.
Verbatim live Job Pipeline stages (from 2026-08-13 startup logs, for the record): New Lead, Intake/Qualification, Estimate in Progress, Quote Sent, Quote Accepted, Job Scheduled, Job In Progress, Job Completed, Invoice Review, Invoice Sent, Paid / Closed Won, Closed Lost (Declined). 12 stages; no "Closed Lost (Cancelled)".
Matt decisions: JOB-1102 cancelled (status_v2='cancelled' set by controller — digest will not fire for it; the two test calendar events remain for manual deletion). Merge to main approved once final review clears.
Final review: FIX WAVE REQUIRED (no correctness Criticals; seams coherent). Controller settled live: F4 digest Slack leg VERIFIED (synthetic JOB-1103 → ops-test post, stamp, idempotent, cleaned up, secret restored); F6 job_events_status_check allows 'skipped' (already verified earlier); Airtable Field Registry row added (Gtl6ADpbBGOlYYFil4n6 reuse). Fix wave dispatched (1 agent, 8 items incl. F5 loud missed-job logging + F7 clean success rows + ledger committed to docs/). Triage accepted as reviewed: follow-ups to backlog, drops dropped.
