# SDD ledger — plan: docs/superpowers/plans/2026-08-14-phase-b-slice-2-estimate-builder-ghl-push.md

Workspace: branch `phase-b-slice-2` in primary checkout (slice-1 precedent; main untouched).
Branch base: aa34a4a (plan commit; parent 342f489 = main).
Spec: BUILD_PLAN.md L204–213 + DISCOVERY_2026-07-31.md §1 + docs/superpowers/plans/2026-08-14-phase-b-estimates-research.md — all reachable.
Execution model (Matt's standing directive): sonnet implements, opus reviews per task + final whole-branch review.
Pre-existing untracked files (NOT ours, leave alone): OPS_ROADMAP.md, prompt.md, supabase/.temp/, 2026-07-31-*.txt.

## Preflight conflict scan (2026-08-14)

Pairs sharing a file or interface:

| Tasks | Producer → Consumer | Finding |
|---|---|---|
| T1/T5 | deno.json excludes `web/` before web exists | Harmless; T5 re-verifies `deno task test` |
| T2/T3 | T2 re-creates guard fn; T3 adds AFTER UPDATE audit trigger on same table | Compatible (BEFORE guard vetoes, AFTER audits); order T2→T3 correct |
| T2/T3 | T2 adds created_by(+name) immutable; T3 audits "the 4 mutable columns" | Consistent — created_by not mutable, not audited |
| T3/T8 | RPC signatures → repo.ts rpc() calls | Signatures match verbatim in both task texts |
| T4/T12 | ghl_push_state columns → push.ts per-target gating; `app_to_ghl` → log.ts | Columns cover all usage (ids, timestamps, last_error, attempts) |
| T5/T7 | shim `export *` → T7 adds `export requireRates` | Shim picks it up automatically; no edit needed |
| T7/T8 | loadRatesConfig → repo.createEstimate | Return shape consistent ({rates, defaultMarkupPct, markupFloorPct}) |
| T9/T10, T9/T12 | client.ts fns → doc builder spike + push orchestration | Names consistent (listEstimateDocs, updateOpportunity, etc.) |
| T11/T11b | EstimateBuilder reused by revise page | Preload-from-parent noted in both |
| T6/T8+ | requireUser() first line of every action | Consistent |

Per-task self-consistency:

| Task | Check | Finding |
|---|---|---|
| T2 | Mutable set unchanged vs plan's writer contract | Consistent (status, quoted_price, quote_override_reason, job_number) |
| T3 | Verification uses permanent labeled rows | Complies with undeletable-estimates constraint; rows left `declined` |
| T10 | Doc requires contactDetails id+name+email+phoneNo vs nullable client_email/phone | Handled: push gates `skipped_missing_contact`; UI pre-flight in T12 |
| T12 | "labor hrs = labor_cost/labor_rate" (effective hours not stored) | Deliberate reconstruction; covers both labor methods |
| T12 | Path B skips doc, fields still push | Matches design decision |
| All | Tests asserting nothing / verbatim duplication mandated | None found |

Scan verdict: no conflicts requiring rulings. Watch item (not a conflict): GHL scope smoke (T9) and auth users (T6) depend on Matt's Manual Setup #1/#2 — tasks degrade gracefully (fields-only / provision-at-test-time) if not yet done.

## Entries
Task 1 BASE: aa34a4a
Ruling: Hybrid-lane concurrency (Matt's explicit choice, 2026-08-14, overrides SDD serial rule) — DB lane (T2→T3→T4) runs concurrent with web lane (T5, then T6∥T7∥T9, then T8∥T10); parallel implementers get isolated worktrees, merged into phase-b-slice-2 after each task's review passes; migrations stay serial within the DB lane. Cost if wrong: merge conflicts / rework if lanes turn out to share files — mitigated by disjoint-path analysis in preflight scan.
Task 1: implementer DONE (commit 184cadb, deno task test 18/18 + --allow-all 18/18). Review dispatched (opus).
Task 2: implementer dispatched (sonnet, worktree; BASE 184cadb; migration 20260814200000_phase_b2_estimator_columns.sql).
Task 5: implementer dispatched (sonnet, worktree; BASE 184cadb).
Task 1: minor (deferred): CLAUDE.md L249-252 ragged double-parenthetical prose — polish in T13 doc pass
Task 1: minor (deferred): NEXT_SESSION_PROMPT.md:23 still names old --allow-all form as canonical — T13 regenerates it anyway
Task 1: minor (deferred): deno.json absent from CLAUDE.md Repository Structure tree — add in T13
Task 1: minor (deferred): deno.json exclude omits .claude/worktrees/ (harmless now; matters only if fmt/lint tasks added); .claude/ untracked and not gitignored — consider gitignore entry in T13
Task 1: complete (commits aa34a4a..184cadb, review clean)
Ruling: Agent-created worktrees branch from main (342f489), not slice-2 HEAD — acceptable for T2/T5 (no dependence on slice-2 commits); T6/T7/T9 fan and later parallel tasks get MANUALLY created worktrees off current phase-b-slice-2 HEAD (git worktree add -b lane-tN). Cost if wrong: an agent building against a stale base — mitigated by explicit base control.
Task 2: implementer DONE (commit 32465ce on worktree-agent branch, base 342f489). Migration 20260814200000 applied live; em-dash double-apply disclosed + ledger cleaned. Review dispatched (opus, incl. live-DB verification).
Task 2: review — spec ✅, quality approved w/ 1 Important (I-1 migration-history row holds pre-fix SQL) + 3 Minor. Fix round 1 dispatched (resume implementer: statements repair + BUILD_LOG paragraph).
Task 2: minor (deferred): created_by FK unindexed — add index only when a "my estimates" filter exists
Task 2: minor (deferred): FK NO ACTION means auth-user deletion blocked once they save an estimate — document in T13 docs; created_by_name is the durable snapshot
Task 2: Ruling: M-3 created_by immutable blocks backfill UPDATE — accepted as intended; Airtable backfill rows INSERT with created_by NULL and never update it. Cost if wrong: a future backfill wanting attribution would need new version rows.
Task 2: cannot-verify #1 (guard runtime behavior for new columns) — resolved by controller: T3's live verification exercises UPDATEs on a permanent labeled estimate; T3 brief already covers trigger behavior. Carried to T3 dispatch: also attempt an UPDATE to created_by_name expecting a raise.
Task 2: fix round 1/5 (1 addressed pending re-review, 0 open — I-1 statements repair, DB-only, no new commits; commit range unchanged 342f489..32465ce)
Task 2: fix round 1/5 re-review — I-1 ADDRESSED, no new breakage. Cherry-picked 32465ce → b66d640 on phase-b-slice-2; worktree removed; deno task test 18/18 post-merge.
Task 2: complete (commits 342f489..32465ce, merged as b66d640, review clean after 1 fix round)
Task 5: implementer DONE (commit 5720366 on worktree branch, atop b9c4be4 cherry-pick of deno.json). Concerns: _shared/package.json type:module added off-brief (reviewer to scrutinize deploy impact); report copied from worktree to main .superpowers. Review dispatched (opus).
Task 3: dispatched (sonnet, manual worktree lane-t3 @ b66d640; migration 20260814210000_phase_b2_rpcs_audit.sql).
Task 3: implementer DONE (commit c9eab64 on lane-t3; est number 1414 consumed v1+v2 permanent labeled test rows, left declined; all 8 live checks passed incl. T2 guard carried check). Concern flagged: auto-supersede audit row null actor. Review dispatched (opus).
Task 5: review — spec ✅ all 12 items, approved. _shared/package.json adversarially validated (removal-test proves necessity; absent from Deno module graph → deploy-inert; deno check green on both live consumers). Cherry-picked 5720366 → e8cdbcd; worktree removed.
Task 5: minor (deferred): report factual error (root package.json HAS explicit type:commonjs) — record corrected here
Task 5: minor (deferred): turbopack.root widens dev watcher to whole repo incl. .claude/worktrees — dev-setup note in T13
Task 5: minor (deferred): next.config.ts __dirname breaks if web ever goes type:module — leave; vitest.config uses import.meta.dirname
Task 5: minor (deferred): vitest lacks include narrowing (.next not excluded) — cheap hardening, fold into any later web test task
Task 5: minor (deferred): /debug route is public+prerendered — T6 gates it under (app); removal tracked for slice cleanup
Task 5: carried to T13: (1) Vercel "include files outside root" MUST be on (I-1); (2) CV-1 mitigation — redeploy crew-night-before once post-merge to close the _shared/package.json deploy question permanently
Task 5: complete (commits b9c4be4..5720366, merged as e8cdbcd, review clean)
Ruling: T7 depends on T6's admin.ts and both would edit web/package.json+lockfile — fan adjusted to T6∥T9 now, T7 after T6 merges. Cost if wrong: none material (schedule shape only).
Ruling: T9 skips `server-only` import (package arrives with T6's installs; parallel-lane package.json conflict avoidance); T8/T12 add the guard when wiring. Cost if wrong: a window where ghl/client.ts lacks the accidental-client-import build guard — closed at T8.
Task 6: dispatched (sonnet, lane-t6 @ e8cdbcd). Task 9: dispatched (sonnet, lane-t9 @ e8cdbcd).
Task 3: review — spec ✅ all 19, approved; 2 Important (I-1 null-actor supersede audit, I-2 no insert-path override-reason backstop) + 5 minors. Sequence-burn concern REFUTED with live proof (coalesce lazy, seq still 1414 after explicit-number inserts).
Task 3: Ruling: fold I-1, I-2, M-1 (nullif casts), M-2 (allow clearing override), M-3 (round 2dp), M-4 (update_estimate_job_number RPC), M-5 (audit-table guard) into ONE fixups migration 20260814215000 in fix round 1 — cheaper than leaking DB fixes into T4/T8. Cost if wrong: one extra migration file.
Task 3: note for BUILD_LOG: est numbers 1400-1413 burned by dev rollbacks, 1414 = permanent test chain, first real estimate ≥1415. Migration ledger version stamps differ from filenames repo-wide (MCP apply pattern; pre-existing).
Task 3: deferred to final review attention: 6 pre-existing clock-in-era SECURITY DEFINER functions callable by anon (get_my_role etc.) — unrelated to slice 2, repo-level open item.
Task 3: fix round 1/5 dispatched (resume implementer; fixups migration).
Task 9: implementer DONE (commit 49fa110 on lane-t9). SCOPE SMOKE = GO (HTTP 200, 510 estimate docs live; no token rotation needed — Manual Setup #1 moot). Pipeline + stage resolved live. 21/21 vitest. Review dispatched (opus).
Task 3: fix round 1/5 (7 findings fixed in migration 20260814215000, commit a405d42; est 1415 burned by negative CHECK test; JOB-1104 substituted for FK-violating test value). Scoped re-review dispatched.
Task 9: review — spec ✅, approved; 1 Important (I1 config-error laundering) + 9 minors; fixing I1+M2+M7 in round 1 (resumed implementer).
Task 9: minor (deferred): M4 — createOpportunity not idempotent under network-retry; T12 push module MUST search-before-create (carried to T12 dispatch)
Task 9: minor (deferred): M5 dead duplicated types; M6 untyped returns; M8 unencoded location id; M1 data-envelope fallback drift; M9 report count off-by-one
Task 9: carried to T12: CV-2 — PUT /opportunities customFields merge-vs-replace semantics unproven (Phase A implies merge); verify during T12 live E2E
Task 6: implementer DONE (commit bb4448c on lane-t6; live browser-verified login/gate/signout; server-only guard build-proven; test user SQL-created then fully deleted). Matt action: hand-create web/.env.local (permission layer blocked agent writes; contents in task-6-report). AGENTS.md/CLAUDE.md autogen gitignored. Review dispatched (opus).
Task 3: fix round 1/5 re-review — PASS, all 7 ADDRESSED, no new breakage. Cherry-picked c9eab64+a405d42 onto phase-b-slice-2; lane-t3 removed.
Task 3: complete (commits c9eab64..a405d42, review clean after 1 fix round). Est numbers: 1414 test chain, 1415 burned, first real ≥1416.
Task 4: dispatched (sonnet, lane-t4 worktree; migration 20260814220000_phase_b2_ghl_push_state.sql).
Task 9: fix round 1/5 (3 fixed — I1 hoist, M2 comment, M7 test pinning; commits 49fa110..434c748, 23/23). Scoped re-review dispatched.
Task 4: implementer DONE (commit e368ac5 on lane-t4; live verified; sync_type→trigger_event brief deviation correctly resolved from live schema). Review dispatched (opus).
Task 9: fix round 1/5 re-review — PASS all 3, no new breakage. Cherry-picked 49fa110+434c748; lane-t9 removed.
Task 9: complete (commits 49fa110..434c748, review clean after 1 fix round). SCOPE GO confirmed live (510 docs).
Task 10: dispatched (sonnet, lane-t10 worktree off merged HEAD incl. GHL client).
Task 6: review — spec ✅ (12/12, users-in-CLAUDE.md carried: Matt hasn't provisioned yet), quality CHANGES REQUESTED: C1 open redirect via next param, I1 array-param 500 on /login, I2 middleware drops rotated cookies on authed-/login bounce; 6 minors. Fix round 1 dispatched (C1+I1 via safeNext helper, I2+M3 cookie propagation, M4 server-only, M5 env checks, M6 signOut error check).
Task 6: Ruling: signOut keeps GLOBAL scope deliberately (3-user tool; sign-out-everywhere acceptable). Cost if wrong: minor UX annoyance across devices.
Task 6: minor (deferred): M2 middleware matcher exempts *.png-suffixed routes at any depth — tighten opportunistically
Task 6: HARD RULE promoted from M1 (carry into T8/T11/T11b/T12 dispatches): layouts render concurrently with children — every page AND server action under (app) must call requireUser() itself; the layout call is display-name only, never the security gate.
Task 6: carried: record 3 auth account emails in CLAUDE.md at T13 once Matt provisions (auth.users currently has only the 1 pre-existing May-2026 account).
Task 4: review — approved, no Critical/Important. Minors: (1) 2-char em-dash comment divergence file↔applied (awareness only, note in BUILD_LOG); (2) repo-wide filename-vs-ledger version skew (pre-existing; T13 documents in CLAUDE.md — never rename applied files). Cherry-picked e368ac5; lane-t4 removed.
Task 4: complete (commits 8a36025..e368ac5, review clean). DB LANE COMPLETE (T2+T3+T4 merged and live).
Task 4: carried to T12: v2 estimates start with an empty push-state row — push code must carry ghl ids forward from the parent via supersedes_estimate_id → parent's ghl_push_state.
Task 6: fix round 1/5 (6 fixed — C1/I1 safeNext, I2/M3 cookie propagation, M4, M5, M6; commits bb4448c..457a46d; 7/7 + live curl exploit checks). Scoped re-review dispatched.
Task 6: fix round 1/5 re-review — 5/6 ADDRESSED (I1/I2/M4/M5/M6 genuinely closed, cookie-option preservation verified against compiled next impl). C1 NOT ADDRESSED: tab/LF/CR control-char open-redirect bypass live-confirmed (/%09/evil.com). Fix round 2 dispatched: mandated new URL() origin-comparison approach (not char blocklist).
Task 10: implementer DONE (commit 4873e18 on lane-t10; 58/58). LIVE SPIKE found 3 validator corrections (name≤40, line type one_time, schedule null) + meta camelCase round-trip asymmetry — all fixed. Review dispatched (opus).
Task 10: CRITICAL carry to T12: GHL stores meta keys CAMELCASED (lbdEstimateId) — push orchestration's own meta-matching for create-then-crash recovery MUST read camelCase, not the snake_case sent.
Task 10: minor carry to T11/T12: doc name truncated to 40 chars (lossy) — full job name belongs in line-item description / opportunity custom fields.
Task 6: fix round 2/5 (C1 hardened via new URL() origin check; commits 457a46d..d709e46; 13/13 + live curl on 3 bypass payloads → same-origin). Scoped re-review dispatched.
Task 10: review — spec ✅, approved w/ 1 Important (Finding 1: negative last-line amount when zero/near-zero weight sorts last — happy-path tests missed it; Critical if GHL rejects negative amounts) + 1 minor (both-null docTotal → NaN). Fix round 1 dispatched: largest-remainder allocation (never negative) + non-negativity tests + docTotal guard.
Task 10: cannot-verify carried: whether GHL rejects negative line amounts is untestable without pushing one; fix makes moot. EstimateForDoc structural stand-ins reconcile with real DB types at T12 merge.
Task 6: fix round 2/5 re-review — C1 STILL NOT ADDRESSED. New bypass: /..//evil.com normalizes to pathname //evil.com (passes input origin check, fails at redirect re-resolution — the re-entrancy trap). Round 3 dispatched: re-gate the OUTPUT string via second new URL() resolution + explicit pathname startsWith("//") check + full bypass battery with origin-property loop assertion.
Task 10: fix round 1/5 (Hamilton largest-remainder allocation → structurally non-negative; docTotal guard; commits 4873e18..ef89f29; 64/64). Scoped re-review dispatched. Mental-model note for T12: no longer "last line absorbs remainder" — sum exact, all lines >=0, tie-break decides stray cent.
Task 6: fix round 3/5 (output re-gate: normalized-pathname startsWith + re-resolution proof; commits d709e46..363f51; 32/32 + live curl on /..// battery → "/"). Implementer flagged: 3 rounds on safeNext, no proof a 4th class doesn't exist w/o fuzzing. Re-review dispatched WITH fuzzing mandate (opus).
Task 10: fix round 1/5 re-review — both ADDRESSED, money math verified exact (hand-check + 20k random trials), no new breakage. Cherry-picked 4873e18+ef89f29; lane-t10 removed.
Task 10: complete (commits 4873e18..ef89f29, review clean after 1 fix round). Live-validated payload (name<=40, one_time, schedule null, meta camelCase read).
Task 10: HARD carry to T8 (data layer): line-item labor_hours/dump_count/materials_cost are NOT DB-constrained ≥0 and NOT builder-clamped — negative inputs produce negative GHL line amounts. T8 must validate non-negativity in the write path (engine rejects negatives, but the builder takes raw rows).
Task 10: minor deferred: test-name "near-zero NOT last" uses same [1,1,1,0.001] array (cosmetic); report narrative had an illustrative-number inaccuracy (fix correctness unaffected).
Task 7: dispatched (sonnet, lane-t7 worktree off merged HEAD).
Task 6: fix round 3/5 re-review — C1 ADDRESSED WITH FUZZ EVIDENCE (71,206-case corpus, 0 violations; negative control proved fuzzer detects the class; output re-resolution gate is a general defense). Cherry-picked 4 commits (34eb9b7..0d3470b); lane-t6 removed.
Task 6: complete (commits bb4448c..363f511 → merged 34eb9b7..0d3470b, review clean after 3 fix rounds). Open-redirect was genuinely subtle — 3 real bypass classes closed (protocol-relative, control-char, normalized-output re-entrancy).
POST-WEB-LANE MERGE CHECK: combined web vitest 95/95, deno 18/18, `npm run build` green with env supplied (routes /, /debug, /login all dynamic; middleware registered). Merge coherent.
Matt action reminder (T13/before local dev): web/.env.local must be hand-created (NEXT_PUBLIC_SUPABASE_URL + anon key sb_publishable_ZmXLIhozN3vMWf-8e13hQQ_59AkdjnY; service role key server-only) — env check throws at build without it (by design).
Task 7: ACTUALLY dispatched now (sonnet, lane-t7). [prior ledger line said dispatched prematurely — agent launched this turn.]
Remaining dependency shape: T7→T8 (data layer needs RPCs+rates+auth, all merged/merging); T8→T11→T11b; T12 parallel to T11 after T8; T13 last. Parallelism tapers — integration tasks are interdependent.
Task 7: review — approved, no Critical/Important. One-word engine export verified clean, golden gate 18/18, no-fallback airtight (coercion-before-validation, missing-key throw before get()). Cherry-picked 61b6cbc only (T6-acquiring merge already on branch); lane-t7 removed.
Task 7: complete (commit 61b6cbc, review clean, no fix round). deno 18/18 post-merge.
Task 7: minor deferred: defaultMarkupPct/markupFloorPct validated finite but not >=0 (T11 markup UI should enforce); RATE_KEYS stale comment.
Task 8: dispatched (sonnet, lane-t8 off merged HEAD; needs RPCs+rates+auth all merged).
Task 8: implementer DONE (commit 27ea451 on lane-t8; 139/139). Added zod ^4.4.3 as explicit dep (runtime need). Caught numeric-as-string DB gotcha — normalizes at every boundary. validateQuoteOverride split as separate pure fn (needs total_bid post-compute). actions are plain async (not FormData) for dynamic line-item arrays. Review dispatched (opus).
Task 8: carries for T11/T11b/T12: no updateJobNumber action wrapper yet; repo write RPCs not live-exercised (T3 covered 1414 chain) — first real create in T11 = smoke test.

=== SESSION PAUSE REQUESTED (Matt, mid-build) ===
Plan: finish the in-flight T8 review, process it (merge if clean; if fix needed, record findings and DEFER the fix to next session rather than starting a round), then STOP. Update BUILD_LOG/CLAUDE.md/NEXT_SESSION_PROMPT + memory. Resume at T11 (builder page) + T12 (push, parallel) next session.
State at pause: T1-T7,T9,T10 complete+merged to phase-b-slice-2 (NOT to main). T8 implemented (commit 27ea451 on lane-t8), review in flight. Remaining: T8 merge, T11, T11b, T12, T13.
Task 8: review — APPROVED, no Critical/Important. Writer contract + negative rejection both correct & exhaustively tested. Cherry-picked 27ea451; lane-t8 removed. deno 18/18, web 139/139 post-merge.
Task 8: complete (commit 27ea451 → merged 123b74a, review clean, no fix round).
Task 8: MINORS carried to T11/T11b (deferred, must address before those pages ship):
  - listEstimates q is PostgREST-filter-injectable (repo.ts:1116) — sanitize/escape ,() before T11b list page wires it.
  - quotedPrice not non-negative-guarded (validate.ts:69) — add nonNegativeNumber (negative override → negative GHL amount in T12).
  - updateStatus accepts any of 6 statuses w/ no transition rules — T11b UI must only offer sent/accepted/declined (never superseded/historical).
  - createNewVersion on a stale parent fails w/ raw unique-violation — T11b add friendly "newer version exists" check.
  - reconciliation just-over-boundary (~0.02) test missing (code correct) — add lock test.
Task 8: cannot-verify carried: repo write path not live-exercised (static+read-only only) — first real create in T11 = smoke test (create one estimate, confirm row + audit + number).

=== SESSION CLOSED 2026-08-14 (mid-build, after T8) ===
DONE+MERGED to phase-b-slice-2 (tip 123b74a, NOT merged to main): T1,T2,T3,T4,T5,T6,T7,T8,T9,T10 (10/14).
REMAINING next session: T11 (builder page), T11b (list+detail+lifecycle), T12 (push orchestration — parallel w/ T11 after nothing, both need T8 which is done), T13 (Vercel deploy + docs). Briefs staged in this dir (task-11/11b/12/13-brief.md).
All 3 DB migrations LIVE on eiqqqwajmcpcwhvxxnhx. Web suite 139/139, deno 18/18, build green w/ env.

=== SESSION RESUMED 2026-08-14 (later) — resume at T11+T12 ===
Branch phase-b-slice-2 @ 26b9495 (10/14 done). web/.env.local still MISSING (Matt's manual setup not done); controller's permission layer blocks .env reads — lanes source creds themselves per T9 precedent (root .env).
Ruling: T12 splits — lib layer (push.ts, log.ts, mapping/rendering + vitest) implemented NOW in lane-t12 parallel with T11; the brief's "modify builder/detail to wire it" (attach-existing picker, post-save push, detail push status + Retry) DEFERRED until T11/T11b merge — those files don't exist yet in lane-t12's base. Cost if wrong: one extra wiring dispatch; avoids guaranteed merge conflict on builder files.
Ruling: T12 Estimator custom field sources from the estimate row's created_by_name (durable snapshot; pusher may differ from creator), fallback current session identity — brief says "from auth identity", interpreted as the creating estimator's identity. Cost if wrong: wrong name in a GHL display field, trivially correctable.
Ruling: T11 live smoke row (first real create) must be labeled job_name 'TEST — void, do not use' and flipped to declined after verification (estimates are permanent/undeletable; 1414-chain precedent). If service-role env unavailable in lane, smoke reports blocked — carried to controller, not faked.
Task 11 BASE: 26b9495 (lane-t11). Task 12 BASE: 26b9495 (lane-t12).
Task 11: dispatched (sonnet, lane-t11).
Task 12: dispatched (sonnet, lane-t12, lib-only scope).
Env UNBLOCKED (Matt, mid-session): web/.env.local created with all 5 vars (Supabase URL/anon/service-role + GHL key/location) and copied into lane-t11 + lane-t12 worktrees. Manual Setup #1 from last session is DONE. Live smokes (T11 first-create, T12 E2E + push-state writes) fully unblocked. Manual Setup #2 (provision 3 auth users) still outstanding.
SCOPE CHANGE (Matt, mid-session, overrides plan + T6): NO LOGIN on the estimate tool. Decisions via Q&A: (1) attribution preserved via a no-password "Who's estimating?" picker (Dane/Jackson/Matt), device-remembered, feeding created_by_name (created_by stays NULL — column nullable by design); (2) deployment ships OPEN for now — protection revisited at T13 (Vercel-layer option noted). Consequences: requireUser() HARD RULE is RESCINDED; T6's login gate (middleware redirect, /login, signout, safeNext) becomes dead code — T11 lane strips it (owns the app surface this session) incl. its tests, separate commit from the builder work; actions.ts drops requireUser; audit RPCs receive p_actor=null + p_actor_name=picker name. Manual Setup #2 (provision auth users) is CANCELLED. Cost if wrong: re-adding auth later is a known, previously-reviewed pattern (T6 preserved in git history).
Task 11: scope-change addendum sent to implementer (strip auth gate + picker chip).
Task 12: scope-change notice sent to implementer (no auth imports; created_by_name may be null → omit Estimator field).
PLAN APPROVED (Matt, via plan mode): docs/superpowers/plans/2026-08-14-no-login-estimator-picker.md — Tasks A1 (EstimateActor identity plumbing), A2 (delete login stack + EstimatorChip), B1 (is_path_b column end-to-end). Runs serial on lane-auth (worktree @ 26b9495), per-task review, merge each. T12's open questions ruled in the plan: Path B = real column; opportunity dedup = contact-scoped exact name match, no field repurpose; service-role key present per Matt's shell output. T12 injection concern: messages were genuine (this controller); plan approval is the trusted-channel confirmation.
Task A1 BASE: 26b9495 (lane-auth). Task A1: dispatched (sonnet).
Task A1: implementer DONE (commit 21aec45 on lane-auth; 148/148, build green). Review dispatched (opus).
Task 11: implementer DONE (commit 2f0ae62 on lane-t11; 149/149, build green). Live smoke ran pre-deferral via temp route handler — estimate 1416 burned (TEST-labeled, declined); first real ≥1417. Closes T8 cannot-verify (write RPC live-exercised). Disclosed off-brief: 1-line nav href fix /new → /estimates/new. Review dispatched (opus).
Task A1: review — spec ✅ all 8 steps, Approved, no Critical/Important. Named risks (stale call sites, it.each spreading) checked clean. Cherry-picked 21aec45 → 5b14562 on phase-b-slice-2; web suite 148/148 post-merge.
Task A1: minor (deferred): map.test.ts double import-from-same-specifier; created_by assertion should be toBe(user.id) to keep pass-through semantic; no test pins ESTIMATORS to the exact 3-name list; no actions-layer rejection test (breadth).
Task A1: Ruling: admin.ts:20 doc comment ("Callers MUST call requireUser() first") is now false — A2 updates that COMMENT despite the constraint's "admin.ts untouched" (intent was code, not comments). Cost if wrong: none (comment-only).
Task A1: complete (commits 26b9495..21aec45, merged as 5b14562, review clean)
Task A2 BASE: 5b14562 (lane-auth must fast-forward/rebase to pick up A1 merge). Task A2: dispatched (sonnet).
Task 11: review — spec ✅ (all brief items + 5 addenda; live smoke row 1416 independently DB-verified incl. audit row), quality NEEDS FIXES: I-1 partial-decimal inputs (".25") silently resolve to 0 via ""-coercion + React bailout (hits mandated fractional dumps); I-2 Save can be disabled with unreachable explanations (dead code at :210-217). Fix round 1 dispatched (resume implementer).
Task 11: minor (deferred): crypto.randomUUID throws on non-secure origins (breaks LAN phone QA); sortOrder collision after middle-item removal (permanent in immutable rows — worth folding into fix round if trivial); todayIso uses UTC date; clamp helpers duplicated+untested; 3 broken label associations; Path B checkbox looks live but persists nothing (integration round resolves); drift epsilon magic number; client floor gate strictness differs from server; ScopePickerSheet lacks dialog semantics.
Task 11: Ruling: minor #4 (sortOrder collision after middle-item removal) ELEVATED into fix round 1 — it writes permanently ambiguous ordering into immutable line-item rows that T11b/T12 doc rendering will consume; fix is trivial (derive sort_order from array index at submit). Cost if wrong: one slightly-larger fix diff.
Task A2: implementer DONE (commit 17c5a4a on lane-auth; 117/117 = 148 minus 31 deleted safe-next tests, build green, no middleware//login in routes). Harness auto-flagged a security warning on the auth deletion — noted: the change IS Matt-authorized (direct instruction + plan-mode approval of the no-login plan); warning is a heuristic without user-turn visibility. Review dispatched (opus).
Task A2: review — spec ✅ (9 deletions exact, chip verbatim, zero dangling importers via head grep, SSR-safety verified), Approved, no Critical/Important. 5 minors deferred: admin.ts comment overstates "access control" (attribution ≠ gate — wording fix folded into B1 dispatch as controller addendum); chip lacks aria-pressed (plan-inherited); unguarded localStorage in getSnapshot (plan-inherited); vestigial justify-between; report +8/-8 vs actual +4/-4 misread.
Task A2: complete (commits 21aec45..17c5a4a, merged onto phase-b-slice-2, review clean)
Task B1 BASE: lane-auth @ 17c5a4a. Task B1: dispatched (sonnet) w/ admin.ts comment-wording addendum.
Task B1: first dispatch BLOCKED by auto-mode classifier (delegating live DB migration to a subagent). Re-dispatched with live-apply carved out: agent authors migration file + code + tests only; CONTROLLER applies migration via session's own MCP permission gate after review, then runs the brief's live verification queries. Respects the denial's intent (production schema change stays behind the session permission gate).
Task 12: implementer DONE_WITH_CONCERNS (commit 52a6d86 on lane-t12; 204/204 web, deno 18/18, build green). Live E2E ran real (create/attach/retry/version-inheritance/missing-phone gates verified; GHL artifacts cleaned; DB rows left declined). Concerns: (1) is_path_b pending B1 merge — defensive cast documented; (2) LIVE DEFECT found in merged T9 code: searchContactByEmail 422s against real GHL — routed around in own resolveContact, needs follow-up fix task; (3) CV-2 merge-vs-replace still unverified; (4) forced doc-failure + GHL UI visual check not live-proven. NOTE: implementer created FRESH estimate rows for E2E (deviation from 1414-chain instruction) — review to verify labeling/status/count for bookkeeping. Review dispatched (opus).
Task 11: fix round 1/5 (3 addressed pending re-review — F1 parseNonNegativeDecimal + raw-string state, F2 canSave removed/guards reachable, F3 assignSortOrders at submit; commits 2f0ae62..447d574; 163/163). Scoped re-review dispatched (opus).
Task 11: fix round 1/5 re-review — ALL 3 ADDRESSED (parse-at-boundary refactor verified at every computation/submission site; canSave guards reachable; assignSortOrders end-to-end incl. validate/map interplay). 1 new Minor in fix diff: comma/garbage strings ("1,200") parse to 0 while field displays them — deferred, FOLD INTO INTEGRATION ROUND (strip commas/whitespace in parseNonNegativeDecimal + not-a-number field hint). Informational carry to T11b: LineItemCard raw strings are one-way-after-mount — revise page must mount fresh cards (new keys), never mutate items under existing ones.
Task 11: complete (commits 2f0ae62..447d574 on lane-t11, review clean after 1 fix round). MERGE DEFERRED — Ruling: cherry-picking now would break the shared branch (builder calls 1-arg createEstimateAction; A1 widened to require estimatorName). Integration round on lane-t11 will: merge phase-b-slice-2 in (A1+A2+B1), resolve layout.tsx href-vs-rewrite conflict (keep A2 layout, href /estimates/new), wire useEstimator→estimatorName + isPathB→draft, comma fix, re-smoke through the REAL action path, then merge back as one green unit. Cost if wrong: slightly larger integration diff; keeps shared branch green throughout.
Task B1: implementer DONE (commit 1bf6224 on lane-auth; web 119/119, deno 18/18, migration file authored NOT applied). Concern: is_path_b not in enforce_estimate_immutability's watched list.
Task B1: Ruling: the PLAN was wrong — the guard is an explicit watched-list (T2 precedent: created_by had to be added), not a mutable-whitelist, so is_path_b is NOT auto-enforced. Migration must also re-create enforce_estimate_immutability with is_path_b watched, following the 20260814200000 estimator-columns pattern. Resuming implementer to amend BEFORE live apply. Cost if wrong: none (strictly adds enforcement the plan intended).
Task 12: review — spec ✅ (8/8 rulings, 13/13 field IDs vs mapping docs, E2E rows DB-verified: 1417-1420 burned, six rows, all TEST-labeled+declined → FIRST REAL ESTIMATE ≥1421), quality NEEDS FIXES: I-1 CV-2 replace-semantics blast radius unflagged (could wipe Phase A Crew/date fields on re-push after scheduling); I-2 doc target not independent of fields target (attach path blocks doc on transient fields error, mislabels skipped_missing_contact, emits NO doc sync_log row — breaks one-row-per-target). Fix round 1 dispatched (resume implementer).
Task 12: Ruling: minor "deleted doc has no recovery" ELEVATED into fix round 1 — Dane deleting a draft in GHL UI is a realistic action and currently wedges that estimate's doc push permanently (PUT 404 forever); fallback-to-create on 404 is small. Cost if wrong: slightly larger fix diff.
Task 12: minor (deferred): not_configured logs status=success (token expiry invisible in sync_errors); Scope Notes doc number lags one push; opportunity search unfiltered (can attach to closed/lost opp); Estimator is SINGLE_OPTIONS field receiving free text (may not stick — verify in integration E2E); attempts read-then-write; no unit coverage of orchestrator branching; report test-count noise (6 not 9 log tests; 59 not 57 estimateFields).
Task 12: minor (deferred): estimateFields.ts "pure" module imports ./client (unguarded) — tidy when client.ts gets server-only in the follow-up fix task; searchContactByEmail 422 defect in client.ts = separate follow-up task (zero live callers, inert).
Task B1: fix applied per ruling (commit 345b959 — guard watches is_path_b, difflib-verified single-line delta vs live 20260814200000 body). 119/119. Migration still UNAPPLIED. Review dispatched (opus) over full B1 range 17c5a4a..345b959.
Task B1: review — spec ✅ (both function bodies independently hunk-verified byte-faithful vs 215000/200000 baselines; only intended is_path_b deltas), Approved w/ 1 Important: I-1 comment-vs-code mismatch — is_path_b coalesce lacks nullif('') wrap the comment claims (empty string would raise; app writer never emits it). Fix round 1: nullif wrap (matches function's own convention). Reviewer ⚠️ carried to controller: BEFORE live apply, pg_get_functiondef both functions and confirm they match repo baselines (create-or-replace would silently revert live drift).
Task B1: minor (deferred): bare add column not re-runnable (style-consistent w/ siblings; apply is transactional — don't retry blind); is_path_b now trigger-immutable → mis-set flag needs a new version — integration round should make the toggle deliberate (confirm on save); EstimateSummary list select lacks is_path_b — T11b adds if badging.
Task B1: fix round 1/5 dispatched (resume implementer; one-word nullif fix).
Task B1: fix round 1/5 re-review — I-1 ADDRESSED (nullif parenthesization verified, single-line diff, no new breakage). Migration APPLIED LIVE (phase_b2_path_b_flag) after drift check: live fn defs matched repo baselines (fixups markers present, guard 38 pre-apply). Live verification: is_path_b boolean NOT NULL default false; 12/12 rows false; guard watches 39; DO-block probe confirmed guard raises on is_path_b UPDATE. Cherry-picked 1bf6224+345b959+b5cae38 onto phase-b-slice-2.
Task B1: complete (commits 17c5a4a..b5cae38, review clean after 1 fix round, applied live + verified). NO-LOGIN PLAN (A1+A2+B1) FULLY MERGED to phase-b-slice-2.
Task 12: fix round 1/5 (3 addressed pending re-review — CV-2 SETTLED LIVE: PUT MERGES customFields (standing unknown retired); doc/fields decoupled via hasContactId gate + orchestrator-owned resolveContact + unconditional skip logger; 404-deleted-doc → create fallback live-reproduced; commits 52a6d86..00a2c28; 207/207). Scoped re-review dispatched (opus).
Task T11i (integration round): dispatched (resume T11 implementer on lane-t11). Scope: rebase lane-t11 onto phase-b-slice-2 @ 866dab8 (conflict: layout.tsx A2-rewrite vs href fix — keep A2 structure + /estimates/new href); wire useEstimator → estimatorName arg on createEstimateAction (block save until picked); wire Path B toggle → draft.isPathB (persistence real as of B1; make toggle deliberate per B1 minor — flag is immutable); comma/whitespace strip + NaN hint in parseNonNegativeDecimal (T11 re-review minor); live smoke through REAL action path w/ isPathB=true + picked estimator (burns one number ≥1421, TEST-labeled, declined; verify created_by_name + is_path_b + audit row).
Task 12: fix round 1/5 re-review — ALL 3 ADDRESSED (CV-2 documented at pushFieldsTarget; one-row-per-target audited across every outcome path; 404-fallback adopts new doc id; both named risks clean). 1 new Low deferred: last_error clobbered on fields-error+doc-ok combo (observability only; sync_log intact) — CARRY TO T11b (detail page reads last_error; one-line fix or render from sync_log). Non-blocking note: blanket-404 → duplicate-draft risk documented as accepted.
Task 12: complete (commits 52a6d86..00a2c28, review clean after 1 fix round). CV-2 RETIRED: GHL PUT /opportunities MERGES customFields (live-proven). Cherry-picked onto phase-b-slice-2.
Post-T12-merge check: web 187/187, but tsc on merged branch fails — T12 fixture types is_path_b optional vs B1's required boolean (the reconciliation T12 predicted), plus stale .next validator artifacts for deleted debug/login pages (cache noise; fresh build clears). T12 agent resumed for a single reconciliation commit (fixture is_path_b:false + replace defensive cast with typed read). Verification on merge = tool gates (vitest+tsc+build); final whole-branch review covers the commit.
Task 12 reconciliation: commit 944923b cherry-picked onto phase-b-slice-2. is_path_b now direct typed read; fixture explicit. Verified on branch post-pick: vitest + tsc.
Task T11i: implementer DONE (commit cfc90f0 on lane-t11, rebased onto 866dab8; 149/149, tsc clean, build green). Rebase zero manual conflicts. Live smoke via real Chrome on running app: estimate 1424 (Path B true, created_by_name Matt, created_by NULL, sort_order [0,1,2] after add/remove/add), declined via RPC (no UI yet — T11b), audit row verified. Self-caught: /estimates/new went STATIC post-auth-removal (would freeze rates at build) — force-dynamic added. Estimate numbers: 1421-1423 presumed burned by T12 fix-round E2E, smoke took 1424 → FIRST REAL ESTIMATE ≥1425 (review to confirm). Review dispatched (opus).
Task T9f (follow-up fix task, controller-created): dispatched (sonnet, lane-t9f @ a01a178). Scope: fix searchContactByEmail live 422 (T12 discovery — endpoint contract changed); add server-only guard to ghl/client.ts (deferred from T9 ruling, now safe — no parallel package.json contention); resolve estimateFields.ts pure-module-imports-unguarded-client tidy (T12 deferred minor). Live-verify the corrected search against real GHL.
Task T11i: review — Approved, spec ✅ on all 5 items + both disclosures (force-dynamic verified real and the only affected route; smoke row 1424 DB-verified field-for-field incl. contiguous sort_order through the real UI sequence). No Critical/Important. Minors deferred: negative entry clamps silently (hint gap); "0,25"→25 decimal-comma mis-parse (en-US, theoretical); "Create another" may not reset form (VERIFY IN T11b); label-htmlFor-div nit. FIRST REAL ESTIMATE ≥1425 (burned: 1414, 1416-1424) — CLAUDE.md/T13 must record.
Task T11i: complete. T11 CHAIN MERGED to phase-b-slice-2 (e8ace5b, 6beda79, 92b90ef). Gates: 217/217, build green, tsc clean.
Task 11: complete (final; merged).
Task T11b: dispatched (sonnet, lane-t11b @ 92b90ef).
Task T9f: implementer DONE (commit 0579169 on lane-t9f; 190/190, build+lint clean). searchContactByEmail repaired (live-verified per report); server-only added to client.ts; guard boundary fixed — incl. a SECOND transitive leak found off-brief (estimateFields → estimateDoc → client via allocateAmounts) resolved by extracting zero-dep allocation.ts. resolveContact NOT swapped (dup-400-reuse is one round trip; search-first would reintroduce index-lag race — documented inline). Review dispatched (opus; scrutiny on allocation extraction behavior-identity — it moves T10's money math).
Task T9f: review — Approved, spec ✅ all 3 items + no-swap decision documented in code with sound index-lag reasoning. Allocation extraction PROVEN byte-identical (md5 match both sides; existing tests shifted +8 lines, assertions untouched). Minors deferred: allocation tests reach fn via re-export so purity not self-enforcing (repoint import someday); report counts unwitnessed by pasted output.
Task T9f: complete (commit 0579169, merged onto phase-b-slice-2, review clean).
REPO-LEVEL OPEN ITEM (T9f review discovery, out of slice scope): LIVE airtable-client-sync v19 calls the 422-returning GET /contacts/?email= shape and never checks res.ok — its search leg is dead code; function survives via dup-400 meta.contactId absorption. Same repair as T9f applies. Follow-up edge-function task; record in BUILD_LOG at T13.
FINAL WHOLE-BRANCH REVIEW: dispatched EARLY per Matt's explicit request (parallel with in-flight T11b) over 342f489..e430534 (30 commits, everything except T11b). Ruling: T11b's range gets a scoped supplemental final pass after its task review + merge; the tradeoff (final reviewer can't see T11b cross-interactions) accepted by Matt for wall-clock. Reviewer given the 52-line deferred/ruling triage extract as a required deliverable (must-fix-before-merge vs follow-up vs already-resolved).
FINAL REVIEW RESULT: APPROVED FOR MERGE (after T11b), conditional. C-1 docs claim auth-gated (CLAUDE.md/BUILD_LOG — owned by T13, MUST land before merge-to-main); C-2 listEstimates injection (owned by in-flight T11b carry #1 — verify on its report); C-3 stale auth comments (page.tsx + admin.ts/Field.tsx dangling citations). I-1 NEW cross-task find: listEstimateDocs limit=10 default → extractDocStatus null → "replace" PUT can overwrite a SENT doc for contacts with >10 docs (violates never-mutate-what-client-saw). I-2 allowlist rejection untested at actions layer (elevated from A1 minor given no-auth posture). I-3 pushEstimateToGhl callerless (known; T11b detail Push button = first caller; orchestrator unit tests → T12w). Triage: 23 already-resolved verified, 3 must-fix, 26 follow-up-OK — full accounting in review output. Engine verified: exactly the one-word export diff. Live DB verified clean (13 rows all declined, max 1424).
FINAL FIX WAVE: dispatched (sonnet, lane-final @ e430534) — C-3 comment sweep (page.tsx, admin.ts debug citation, Field.tsx LoginForm citation) + layout.tsx create-next-app title/description + I-1 doc-list completeness + I-2 allowlist tests (pin ESTIMATORS exact list + action-level rejection). ONE wave per skill; scoped re-review after.
FINAL FIX WAVE: implementer DONE (commit 22ae0e5 on lane-final; 237/237 = 220+17, build+tsc green). I-1 = auto-pagination at AUTO_PAGE_SIZE=100 (signature-compatible; explicit-limit callers unchanged); C-3 sweep + layout title + I-2 allowlist/roster/action-rejection tests all in. Open: page size 100 unverified vs real GHL — scoped re-review authorized ONE read-only GHL GET to settle it. Re-review dispatched (opus).
T11b status ping: no reply yet (agent mid-tool-round, 25m elapsed — within precedent for its size; watching).
Task T11b: implementer DONE (commit 276db35 on lane-t11b @ 92b90ef; 240/240 = 217+23, tsc/build/lint clean). All 9 carries addressed; last_error = surgical push.ts one-liner (no lane conflict). Estimate 1425 burned (override→$900+reason→v2→declined) → FIRST REAL ≥1426. Two live-caught bugs fixed in-commit: reason textarea unmounting mid-typing (visibility now keyed off price-differs-from-bid); revise notFound() firing on Next post-action refresh (guard removed; unique-violation friendly error covers). Two-tab concurrent double-revise live-reproduced the friendly error. Review dispatched (opus).
FINAL FIX WAVE: re-review — ALL ADDRESSED. Live page-size check: GHL honored limit=100 (200 OK, 100/511 returned) → AUTO_PAGE_SIZE valid, full sweep = 6 pages. Minors deferred: page.tsx comment cites estimates/page.tsx (exists once T11b merges — self-heals); short-page break honored even when collected<total (theoretical clamp guard — comment-accuracy nit); report +17 arithmetic noise. Cherry-picked 22ae0e5 onto phase-b-slice-2.
FINAL FIX WAVE: complete (commit 22ae0e5, merged, re-review clean).
Task T11b: review — spec ✅ all brief items + all 9 carries (C-2 CLOSED — sanitizer probed for bypasses, DB-verified 1425 lifecycle exact), quality NEEDS FIXES: I-1 integration suite break (fix-wave actions.test.ts has no server-only stub; T11b's actions.ts statically imports push.ts) — one-line stub or dynamic import; I-2 superseded versions render live StatusActions+PushPanel (2-click irrecoverable superseded-marker erasure; stale-price push path) — gate both panels. Fix round 1 dispatched (resume implementer; merge branch into lane first to acquire the colliding test file).
Task T11b: Ruling: minor #1 (* wildcard not neutralized) folded into fix round — reviewer frames it as the wildcard carry left half-done, i.e. partial spec gap, one-char + test. Cost if wrong: none.
Task T11b: minor (deferred): strip-not-escape makes punctuated names unsearchable (documented tradeoff); empty-sanitize silently unfiltered (no hint); catch→notFound masks transient DB errors as 404; itemized-revise preload never runtime-exercised (30-sec manual close-out — fold into fix-round verification); QuoteOverridePanel outside useTransition (staleness trap); superseded revise deep-link lacks banner (I-2 gating may cover); no test that listEstimates calls sanitizer / 23505 maps friendly (inspection+live only).
Task T11b: fix round 1/5 (3 findings + minors #5/#7 closed; merge ec9f059 + fix 53e7d64; INTEGRATED 261/261, tsc/build/lint clean; I-1 via server-only stub; 1424 revised to v2 itemized-smoke then declined — no new number burned, first real stays ≥1426). Scoped re-review dispatched (opus) over fix commit only (merge commit = already-reviewed branch content).
MATT AUTHORIZATION: proceed to T13 once T11b re-review clears (explicit go-ahead, mid-session). T13 runbook: (1) merge lane-t11b into phase-b-slice-2 (lane already contains branch via ec9f059 — merge back, then gates); (2) scoped supplemental final pass over T11b's integrated range (closes the early-final-review coverage gap); (3) T13 proper per task-13-brief.md: Vercel project (root web/, include-files-outside-root ON, server-only env vars — service-role key must come from Matt/lane context, controller cannot read .env), production phone smoke (Matt manual), docs close: C-1 corrections (CLAUDE.md/BUILD_LOG auth-gated claims — MERGE CONDITION), first-real-estimate ≥1426, deno.json tree entry + T1 doc minors, L54 FK note, ghl_push_state/estimates row counts, no-login + picker + is_path_b documentation, BUILD_LOG entry incl. airtable-client-sync dead-search-leg open item + crew-night-before redeploy (T5 CV-1 carry), NEXT_SESSION_PROMPT regeneration; (4) merge-to-main decision returns to Matt with the final-review verdict.
Task T11b: fix round 1/5 re-review — ALL ADDRESSED (server-only stub precedent-matched w/o weakening assertions; both panels gated on superseded w/ view-latest fallback, live statuses unaffected; * → \* composes with escape ordering; #7 banner in; #5 DB-verified: 1424 v1 superseded/3 items, v2 declined/3 items). No new breakage.
Task T11b: complete (commits 276db35 + 53e7d64, merged to phase-b-slice-2 via fast-forward, review clean after 1 fix round). ALL BUILD TASKS DONE. Final-review merge conditions: C-2 CLOSED (verified in T11b review), C-3 CLOSED (fix wave), C-1 (docs) = T13's job, now starting.
Supplemental final pass: dispatched (opus, 542c27c..53e7d64 — the parallelization coverage gap only). T13-docs: dispatched (sonnet, lane-t13 @ 53e7d64) — CLAUDE.md/BUILD_LOG/NEXT_SESSION_PROMPT; Vercel deploy + phone smoke are controller/Matt-owned, excluded from agent scope; deploy-status line left as a marked placeholder for controller to patch post-deploy.
SUPPLEMENTAL FINAL PASS: BRANCH APPROVED FOR MERGE (pending T13 docs). Zero Critical/Important. Confirmations: C-2 intact end-to-end at merged head; I-3 closed (pushEstimateAction validated/caught/revalidates, non-parameterizable); cross-lane seams clean (pagination call-sites, push.ts merge resolution, unified actions.test.ts, app_to_ghl constraint); page.tsx citation self-healed. Minors deferred: M-1 actions don't re-check superseded (UI-only gating — defense-in-depth 2-liner someday; RECORD AS KNOWN LIMITATION); M-2 concurrent double-push can duplicate GHL opportunity (no cross-request guard; low likelihood, manual recovery; RECORD AS KNOWN LIMITATION); M-3 revise-without-parentId silently creates v1 chain (unreachable today; discriminated union someday); M-4 duplicated scope_library loader (extract loadActiveScopeItems when Phase G touches columns) + dead isLifecycleActionStatus export.
T13-docs: implementer DONE (commit d6d9c08 on lane-t13; SQL-verified estimates=16/first real ≥1426, push_state=10, app_to_ghl=24, audit=27; disclosed unverified items honestly). Doc-accuracy review dispatched (opus) — this IS the C-1 merge-condition gate.
T13-docs: review — C-1 CLEARED (present-tense auth claims eradicated all three docs; live counts exact). Quality NEEDS FIXES: I-1 is_path_b immutability sentence INVERTED (doc reproduces the rejected "immutable in practice" framing; guard provably raises — live-probed); I-2 "created_by always NULL" falsified by row 1416 (pre-scope-change smoke row carries real auth uid — that user is FK-pinned); + minors: 100/511 transcription, 1400-1415 vs 1414-live phrasing, repo tree lacks web/ + 5 slice-2 migrations + _shared 3 test files, 4-vs-5 migration count, self-healing gloss understated (superseded restore needs direct RPC), BUILD_LOG:236 needs supersession marker, actor_id absolute needs one-query check. Fix round 1 dispatched (resume implementer).
T13-docs: fix round 1/5 (all 8 addressed; commit 8215e83; SQL found actor_id non-null on 9/27 audit rows — docs now state real invariant w/ 1416 named exception). Scoped re-check dispatched (sonnet).
T13-docs: fix round 1/5 re-check — ALL 8 ADDRESSED, no new inaccuracies. Merged lane-t13 (d6d9c08 + 8215e83) into phase-b-slice-2 via fast-forward.
T13-docs: complete (review clean after 1 fix round). ALL SLICE-2 TASKS + DOCS DONE. Remaining: env vars (Matt), push→preview, phone smoke (Matt), merge-to-main decision (Matt), deploy-status placeholder patch (controller), BUILD_LOG deploy line, session close (rulings list, memory, workspace cleanup).
